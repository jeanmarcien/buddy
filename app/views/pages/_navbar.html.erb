<nav class="navbar navbar-expand-lg py-4">
	<div class="container">
		<%= link_to 'Buddy.', root_path, class:"logo" %>
		<div class="ms-auto d-flex gap-4">
      <% unless current_page?(new_user_registration_path) %>
        <%= link_to 'Sign Up', new_user_registration_path, class:"btn btn-primary rounded-pill px-4" %>
      <% end %>
      <% unless current_page?(new_user_session_path) %>
			  <%= link_to 'Login', user_session_path, class:"btn btn-primary rounded-pill px-4" %></button>
      <% end %>
		</div>
	</div>
</nav>
