<nav class="navbar py-4">  <!-- Supprimé navbar-expand-lg -->
  <div class="container">
	<%= link_to 'Buddy.', pets_path, class:"logo" %>

	<div class="d-flex align-items-center">
	  <!-- Image de profil -->
	  <div class="me-3">
		<%= image_tag "avatar.jpg", class: "rounded-circle", style: "width: 40px; height: 40px; object-fit: cover;" %>
	  </div>

	  <!-- Bouton burger (toujours visible) -->
	  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	  </button>
	</div>

	<!-- Menu toujours en mode collapse -->
	<div class="collapse navbar-collapse justify-content-end" id="navbarNav">
	  <ul class="navbar-nav">
		<li class="nav-item">
      <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "nav-link" %>
		</li>
	  </ul>
	</div>
  </div>
</nav>
